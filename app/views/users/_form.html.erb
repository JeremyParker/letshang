<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :slack_id %>
    <%= form.text_field :slack_id, id: :user_slack_id %>
  </div>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.select(:team_id, options_for_select(Team.all.map {|t| t.id}), id: :user_team_id)%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
